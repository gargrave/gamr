import React from 'react';

import {APP_TITLE} from '../../constants/appConfig';


const HomePage = () => {
  return (
    <div>
      <h1>{APP_TITLE}</h1>
    </div>
  );
};

export default HomePage;
